<% if @user.present? %>
	<%= 'hello Kamal' %>
  <% @user.each do |user| %>
    <p>
      <%= user.name %>
    </p>
    <p>  
      <%= user.email %>
    </p>
    <h2>
      <%= user.password %>
    </h2>
  <% end %>  
<% end %>  